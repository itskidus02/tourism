import React from "react";
// import bg from "../assets/images/lalibela-bg.jpg";

const Home = () => {
  return (
    <div
      className="bg-cover bg-no-repeat bg-[50%_0] h-[80vh] max-w-full overflow-hidden relative w-[100vw]"
      style={{ backgroundImage: `url(${bg})` }}
    >
      {/* Content of the hero section */}
    </div>
  );
};

export default Home;
